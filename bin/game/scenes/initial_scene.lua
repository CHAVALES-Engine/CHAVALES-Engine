-- Escena de control inicial, solo se entra una vez
scene = { -- entidades en escena
	gizmos = false, -- Debug de las fisicas.
	entities = {
        initialEntity = {
			ddol = false,
			components = {
                InitialTest =
                {

                }
			}
   		}
    }
}
